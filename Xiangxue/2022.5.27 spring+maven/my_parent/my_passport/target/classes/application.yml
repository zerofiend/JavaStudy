server:
  port: 8887
spring:
  thymeleaf:
    cache: true
  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp
  profiles:
    active: mapper  # 表示当前配置文件被加载的时候，会动态关联加载application-mapper.yml文件

